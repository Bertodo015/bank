export class Client {
    name: string;
    cpf: string;

    //é uma função, inicializa propriedades da classe ou executar qualquer configuração necessária
    constructor(name: string, cpf: string) {
        this.name = name;
        this.cpf = cpf;
    }
}

